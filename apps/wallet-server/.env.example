# Wallet MCP Server Configuration

# Master encryption key for wallet (required)
WALLET_MASTER_KEY=your-master-key-here

# Transport mode: "stdio" (default) or "http"
MCP_MODE=stdio

# HTTP port (only used if MCP_MODE=http)
MCP_PORT=3001

# Optional: Wallet name/label
WALLET_NAME=my-wallet

# Cheqd network: "testnet" or "mainnet"
CHEQD_NETWORK=testnet

# Host path for the SQLite wallet database (required when using docker:run:wallet).
# The parent directory is created automatically if it doesn't exist.
# The filename within the directory is used as the database name inside the container.
# Example: WALLET_DB_PATH=/home/you/wallet-data/wallet-db
WALLET_DB_PATH=

# --- Live/e2e test configuration (used via apps/wallet-server/.env.test, not committed) ---

# Set to true to enable tests that call the real Truvera API instead of skipping
TRUVERA_RUN_LIVE_TESTS=false

# Truvera API key for live tests (get one from https://dashboard.truvera.io)
TRUVERA_API_KEY=your-api-key-here

# Truvera API endpoint
TRUVERA_API_ENDPOINT=https://api-testnet.truvera.io

# Optional: explicit issuer DID for live tests; falls back to the first DID on the account
TRUVERA_API_ISSUER_DID=
